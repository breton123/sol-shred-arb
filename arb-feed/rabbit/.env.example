# RabbitStream uses the existing Shyft gRPC x-token from ~/.arb-state007.env
# (SHYFT_TOKEN). Do not copy the token into this tree.
#
# Optional override; default is Frankfurt:
# RABBIT_GRPC_URL=rabbitstream.fra.shyft.to:443
