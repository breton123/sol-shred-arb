# HOT-001 — real route0 economics

`amm2` is banned from route0. The searcher is:

```
trigger N
  → canonical S (LIVE-001 cache)
  → predict S'   (copy; no commit)
  → cycle_size() (frozen)
  → opportunity_t  route_id = 0
```

DLMM uses `dlmm_cache_predict` → `dlmm_apply_swap`.
Pump uses `pump_apply_swap` on a stack copy.

Against `liveuniv.bin` slot 450085375 (DAErPzgi DLMM ↔ BRjMA8UA Pump):

| N | predict | cycle_size |
|---|---------|------------|
| DLMM 0.1 SOL (Y→X) | ok | valid  in=0.133 SOL  out=0.140  gp=0.0064  dir=1 |
| Pump 0.1 SOL (quote→base) | ok | valid  in=0.050 SOL  out=0.051  gp=0.00076  dir=1 |

Canonical Pump and DLMM rows unchanged after both evals.

Toy path: `hot_eval_pool` / `hot_quote_route` on family 0 return no opportunity / fail. `hot_apply_pool` refuses DLMM/Pump.

```bash
./build/hot001 arb-cap/live001/liveuniv.bin
```
